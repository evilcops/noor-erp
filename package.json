{
  "name": "noor-erp",
  "version": "0.1.0",
  "private": true,
  "description": "NOOR ERP — Next.js full-stack app with MongoDB",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "seed": "npm run seed --prefix frontend",
    "reset-admin": "npm run reset-admin --prefix frontend"
  }
}
